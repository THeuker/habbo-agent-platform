# MySQL Bundle

`mysql/` contains database configuration and SQL dumps used by the Agent Hotel stack.

## What it provides

- Base schema/data dumps for Arcturus
- Migration SQL files
- MySQL configuration used in local/compose deployments

## Role in architecture

This bundle stores hotel data and configuration state used by the emulator, MCP tools, portal, and AI integration flows.
